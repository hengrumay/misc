# <Project Name>

> **Fill this in.** This is the project README placeholder from `_template/`.
> Replace every angle-bracketed prompt below, then delete this note.

## Purpose

<What this example demonstrates, in plain language — what someone learns or gets
running by the end. Write it for a stranger: no customer context, no "as we saw
at <client>", no internal environment or workspace names.>

## Prerequisites

- <e.g. a Databricks workspace with Unity Catalog + serverless compute>
- <Databricks CLI configured with a profile — authenticate via a profile, never a
  hardcoded workspace host>
- <any Python / package requirements — see `requirements.txt`>

## How to run

1. <step-by-step, in order>
2. <e.g. `databricks bundle deploy --profile <your-profile>`>
3. <...>

## Synthetic data

<Describe the synthetic / fake data this project uses and the generator that
produces it. State plainly: generated / fictional data only — no real customer
data, no personal data (PII / PHI), no internal workspace / account / resource
IDs, no secrets, and no brand colors or other brand fingerprints.>
